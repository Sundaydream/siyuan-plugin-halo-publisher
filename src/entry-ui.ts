import { init } from './main';

// Initialize the Vue app
// We pass a mock plugin object since it is not used in init based on our inspection,
// or we can establish a communication channel here later.
init({}); 
